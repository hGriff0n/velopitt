---
phase: 5
plan: 3
wave: 2
---

# Plan 5.3: Map Integration (Ride Starts)

## Objective
Integrate Ride Start markers onto the map and add a toggle to the Sidebar to control their visibility.

## Context
- Plan 5.1 (Data Core)
- Plan 5.2 (Sidebar Verification)
- src/app/components/map/map.component.ts
- src/app/components/sidebar/sidebar.component.ts

## Tasks

<task type="auto">
  <name>Map Markers Implementation</name>
  <files>src/app/components/map/map.component.ts</files>
  <action>
    Inject `EventService` and load groups.
    Implement logic to add markers for `startLocation` points using group logos.
    Add `rideStartsShowing` input (signal) and use `effect` to toggle visibility.
  </action>
  <verify>grep "rideStartsShowing" src/app/components/map/map.component.ts</verify>
  <done>Map supports Ride Start markers and toggling.</done>
</task>

<task type="auto">
  <name>Add Sidebar Toggle</name>
  <files>src/app/components/sidebar/sidebar.component.ts, src/app/components/sidebar/sidebar.component.spec.ts</files>
  <action>
    1. Update `sidebar.component.ts`:
       - Add `rideStartsShowing` input and `toggleRideStarts` output (matching the pattern).
       - Add the "Toggle Ride Starts" button to the template.
       - **CRITICAL**: Strictly preserve existing inline template/styles. Do NOT reformat or extract.
    2. Update `sidebar.component.spec.ts` to include a test for the new "Toggle Ride Starts" button. 
    3. Run tests to ensure no regressions.
  </action>
  <verify>ng test --include src/app/components/sidebar/sidebar.component.spec.ts</verify>
  <done>Sidebar has new toggle. Tests pass. Styling preserved.</done>
</task>

## Success Criteria
- [ ] Map shows markers when enabled.
- [ ] Sidebar toggle works and updates map state (via parent, presumed).
- [ ] Existing Sidebar layout is verified by tests.
