---
phase: 5
plan: 1
wave: 1
---

# Plan 5.1: Data Core & Ingestion Logic

## Objective
Establish the data foundation for the Community Calendar. This includes the Typescript interfaces, the JSON schema for groups, and the service logic to load these configs and generate recurring calendar events.

## Context
- .agent/state/SPEC.md (Goal 3)
- src/app/services/ (New EventService)
- src/assets/data/ (New groups directory)

## Tasks

<task type="auto">
  <name>Define Event Data Model</name>
  <files>src/app/models/event-schema.ts, src/app/models/index.ts</files>
  <action>
    Create interfaces for `CyclingGroup` (id, name, logo, links), `RideDefinition` (id, name, time, startLocation: GeoJSON.Point, recurrence), and `CalendarEvent` (id, title, start, end, extendedProps).
  </action>
  <verify>grep "interface CyclingGroup" src/app/models/event-schema.ts</verify>
  <done>Interfaces exist and are exported.</done>
</task>

<task type="auto">
  <name>Scaffold Data Structure</name>
  <files>src/assets/data/groups/README.md, src/assets/data/groups/ac-cycling.json</files>
  <action>
    Create the `groups` directory in assets. Add a README explaining how to add a group.
    Create a mock group JSON (e.g., "AC Cycling") with one weekly recurring ride.
  </action>
  <verify>ls src/assets/data/groups/ac-cycling.json</verify>
  <done>Directory structure exists with valid sample JSON.</done>
</task>

<task type="auto">
  <name>Implement EventService & Recurrence</name>
  <files>src/app/services/event.service.ts, src/app/services/event.service.spec.ts</files>
  <action>
    Implement `EventService`.
    - `loadAllGroups()`: Fetch all group JSONs (might need a manifest or known list for MVP, or just one file for now if listing isn't possible without backend. *Decision*: Create `src/assets/data/groups-manifest.json` keeping list of files).
    - `generateEvents(start: Date, end: Date)`: Logic to take RideDefinitions and create instances for the given range based on "Weekly" recurrence.
  </action>
  <verify>ng test --include src/app/services/event.service.spec.ts</verify>
  <done>Service tests pass, correctly generating weekly events for a given month.</done>
</task>

## Success Criteria
- [ ] `EventService` can load group data from assets.
- [ ] Recurrence logic correctly extrapolates "Every Tuesday" into 4-5 dates for the current month.
- [ ] Data model covers all requirements (Location, Time, Links).
