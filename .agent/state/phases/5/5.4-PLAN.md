---
phase: 5
plan: 4
wave: 2
---

# Plan 5.4: Calendar UI

## Objective
Implement the user-facing Calendar page using FullCalendar and update the Sidebar navigation link.

## Context
- Plan 5.3
- src/app/components/calendar/

## Tasks

<task type="auto">
  <name>Install FullCalendar</name>
  <files>package.json, angular.json</files>
  <action>
    Install `@fullcalendar/angular` and plugins.
    Ensure styles are imported.
  </action>
  <verify>npm list @fullcalendar/angular</verify>
  <done>Packages installed.</done>
</task>

<task type="auto">
  <name>Calendar Component</name>
  <files>src/app/components/calendar/calendar.component.ts, src/app/components/calendar/calendar.component.html</files>
  <action>
    Create `CalendarComponent` with `<full-calendar>`.
    Bind events from `EventService`.
    Add `/calendar` route.
  </action>
  <verify>ng test --include src/app/components/calendar/calendar.component.spec.ts</verify>
  <done>Calendar page exists and shows events.</done>
</task>

<task type="auto">
  <name>Verify Calendar Events</name>
  <files>src/app/components/calendar/calendar.component.spec.ts</files>
  <action>
    Add a test case or manual verification step to confirm that the **mock "Temp Group" ride** created in 5.1 is correctly displayed on the calendar.
  </action>
  <verify>ng test --include src/app/components/calendar/calendar.component.spec.ts</verify>
  <done>Verification confirms event exists for mock data.</done>
</task>

<task type="auto">
  <name>Update Sidebar Link</name>
  <files>src/app/components/sidebar/sidebar.component.ts, src/app/components/sidebar/sidebar.component.spec.ts</files>
  <action>
    Update the "Group Rides + Events" button `routerLink` to point to `/calendar`.
    **CRITICAL**: Strictly preserve existing inline template/styles.
    Update tests to verify the link points to `/calendar`.
  </action>
  <verify>grep 'routerLink="/calendar"' src/app/components/sidebar/sidebar.component.ts</verify>
  <done>Sidebar link updated. Tests pass. Styling preserved.</done>
</task>

## Success Criteria
- [ ] `/calendar` route displays monthly calendar.
- [ ] Sidebar "Group Rides" link points to calendar.
- [ ] Sidebar tests pass (UI integrity).
