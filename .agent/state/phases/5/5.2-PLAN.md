---
phase: 5
plan: 2
wave: 1
---

# Plan 5.2: Calendar UI & Map Integration

## Objective
Implement the user-facing Calendar page using FullCalendar and integrate the event start locations onto the main Map.

## Context
- Plan 5.1 (Data Core)
- src/app/components/map/map.component.ts

## Tasks

<task type="auto">
  <name>Install FullCalendar</name>
  <files>package.json, angular.json</files>
  <action>
    Install `@fullcalendar/angular`, `@fullcalendar/core`, `@fullcalendar/daygrid`, `@fullcalendar/timegrid`, `@fullcalendar/interaction`.
    Ensure styles are imported in `angular.json` or global styles.
  </action>
  <verify>npm list @fullcalendar/angular</verify>
  <done>Packages installed.</done>
</task>

<task type="auto">
  <name>Create Calendar Component</name>
  <files>src/app/components/calendar/calendar.component.ts, src/app/components/calendar/calendar.component.html, src/app/app.routes.ts</files>
  <action>
    Create standalone `CalendarComponent`.
    Render `<full-calendar>`.
    Inject `EventService` and bind `events` input.
    Add route `/calendar`.
  </action>
  <verify>ng test --include src/app/components/calendar/calendar.component.spec.ts</verify>
  <done>Component renders calendar and events from service.</done>
</task>

<task type="auto">
  <name>Map Integration</name>
  <files>src/app/components/map/map.component.ts</files>
  <action>
    Inject `EventService`.
    Add a new Map Layer (Symbol Layer) for `Ride Start Locations`.
    Create a method to aggregate rides by location and display a Popup with the list of rides at that spot.
  </action>
  <verify>ng test --include src/app/components/map/map.component.spec.ts</verify>
  <done>Map shows markers for events. Clicking markers shows ride details.</done>
</task>

## Success Criteria
- [ ] `/calendar` route displays a monthly calendar.
- [ ] Mock events from Plan 5.1 appear on the calendar.
- [ ] `/map` shows markers for ride start locations.
