---
description: run npm test to execute unit tests
---

To run the unit tests for the project, use the following command:

```bash
npm test
```

This will trigger the Angular CLI's test runner (`ng test`).
